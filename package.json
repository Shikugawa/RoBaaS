{
  "name": "RoBaaS",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "parcel build app.js --no-cache -d public --target=node",
    "start-dev": "nodemon --watch config --exec \"npm run build && node public/app.js\""
  },
  "dependencies": {
    "express": "~4.16.0",
    "dockerode": "^2.5.5",
    "swagger-jsdoc": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^1.18.3",
    "parcel-bundler": "^1.9.7"
  }
}
